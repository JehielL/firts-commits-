let altura_cm = 173
let altura_m = 1.73
let peso_Kg = 85.50


let altura_redon = Math.ceil(altura_m)
console.log(altura_redon)

let pesoKg_redon = Math.floor(peso_Kg)
console.log(pesoKg_redon)

let maxValue = Number.MAX_VALUE + 1 === Number.MAX_VALUE

console.log(maxValue)
